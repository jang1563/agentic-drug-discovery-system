# Chain Workspaces

Each chain should define:

- initial state
- allowed tools
- state transitions
- deterministic verifier gates
- soft verifier scores
- success conditions
- failure traps
- candidate reward signals

Initial chain directories:

- `target_id_to_hit/`
- `hit_to_lead/`
- `lead_optimization/`
- `admet_tox/`
- `protein_design/`
- `cell_perturbation/`

